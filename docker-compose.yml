services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: unipod
      POSTGRES_PASSWORD: unipod
      POSTGRES_DB: unipod
    ports:
      - "5432:5432"
    volumes:
      - unipod_pg_data:/var/lib/postgresql/data

volumes:
  unipod_pg_data:
